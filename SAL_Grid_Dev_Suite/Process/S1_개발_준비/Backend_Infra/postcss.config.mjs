// @task S1BI1 - Next.js 프로젝트 초기화 + Tailwind CSS 설정
/** @type {import('postcss').ProcessOptions} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
