# P0 Runtime Baseline — CoCoBot (v4.0)

**생성일**: 2026-04-21
**대상**: https://mychatbot.world/ (프로덕션)
**결론**: **RuntimeFailFlag = true** — 시스템 등급 상한 3/10, 리포트 품질 상한 60/100 자동 적용

---

## P0-1 빌드 검증
- 방식: 프로덕션 URL 생존 = 간접 통과
- 결과: Vercel 배포본이 200 응답 → **PASS**
- 비고: 로컬 `npm run build`는 미실행. 배포 파이프라인 통과가 증거

## P0-2 서빙 검증 (루트)
- `/` → HTTP 200, Navbar + Hero + 라이프사이클 5단계 + 타입 카드 6개 + 스킬 리스트 렌더 → **PASS**

## P0-3 스모크 테스트 (10건)

### 페이지 (5건)
| URL | 상태 | 실체 | 판정 |
|---|:-:|---|:-:|
| `/` | 200 | 완전 렌더 | PASS |
| `/login` | 200 | 스켈레톤만, 폼 0개 — 클라이언트 하이드레이션 실패 | **FAIL** |
| `/skills` | 200 | "스킬 0개 / 불러오는 중..." — API는 14개 반환 중인데 UI 연결 끊김 | **FAIL** |
| `/community` | 200 | "불러오는 중..." 고착 — API는 빈 배열 정상 반환 | **FAIL** |
| `/create` | 200 | "로그인 확인 중..." 고착 — 위저드 미진입 | **FAIL** |

### API (5건)
| URL | 상태 | 실체 | 판정 |
|---|:-:|---|:-:|
| `/api/health` | 200 | `{status:ok, env:ok, supabase:ok}` | PASS |
| `/api/bots/public` | **500** | `봇 목록 조회에 실패했습니다.` | **FAIL** |
| `/api/skills` | 200 | 14 skills | PASS |
| `/api/community` | 200 | `{posts:[], pagination:{total:0}}` | PASS |
| `/api/jobs` | 200 | `{jobs:[], total:0}` | PASS |

## P0-4 스키마↔쿼리 대조 — **체계적 결함 9건**

`mcw_bots` 실제 스키마 (migrations 누적):
```
id, username, owner_id, bot_name, bot_desc, emoji, greeting,
faqs, input_text, category, voice, avatar_url, theme_mode,
theme_color, created_at, updated_at
```

유령 컬럼 참조 전수:

| # | 라우트 | 유령 컬럼 | 근거 | 예상 결과 |
|---|---|---|---|:-:|
| 1 | `app/api/bots/public/route.ts:66` | deploy_url, karma, chat_count, avg_rating | SELECT + ORDER + FILTER | 500 확인됨 |
| 2 | `app/api/admin/bots/route.ts:132` | name, status | SELECT 컬럼 | 500 예정 |
| 3 | `app/api/admin/bots/route.ts:148-152` | status | UPDATE SET | 500 예정 |
| 4 | `app/api/community/madang/route.ts:41` | karma, post_count | SELECT | 500 예정 |
| 5 | `app/api/community/route.ts:97` | karma, post_count | SELECT | 500 예정 |
| 6 | `app/api/community/route.ts:197` | karma | SELECT | 500 예정 |
| 7 | `app/api/community/[id]/comments/route.ts:172` | karma | SELECT | 500 예정 |
| 8 | `app/api/operations/hired-bots/route.ts:64` | name | SELECT | 500 예정 |
| 9 | `app/api/bots/[id]/growth/route.ts:72` | user_id | SELECT | 500 예정 |

> ⚠️ `/api/community`가 "200 빈 배열"을 반환한 이유는 community_posts 쿼리가 먼저 실행되고 빈 결과시 mcw_bots 조인을 건너뛴 것으로 추정. 실제 게시글 1건만 있어도 유령 컬럼 500이 발화됨. **현재 빈 DB가 폭탄을 숨기고 있음.**

## P0-5 환경변수·시크릿
- `/api/health`가 `env:ok + supabase:ok` 반환 → 필수 env 주입 확인 → **PASS**
- 단, 프로덕션에서 `SUPABASE_SERVICE_ROLE_KEY` vs `NEXT_PUBLIC_SUPABASE_ANON_KEY` fallback 동작 여부는 별도 검증 필요

---

## P0 자동 편입 Fail 카드 (S0 Grid에 강제 포함)

| Fail ID | 제목 | Area | Priority | Final Verdict |
|---|---|:-:|:-:|:-:|
| P0-F1 | `mcw_bots` 스키마↔API SELECT 9건 불일치 → 500 발생/예정 | DB | Critical | Fail |
| P0-F2 | `/login` 페이지 스켈레톤 고착 — 폼 미렌더 | FN/UX | Critical | Fail |
| P0-F3 | `/skills` 페이지 API→UI 체인 절단 — 14건 받고도 0 표시 | FN/UX | Critical | Fail |
| P0-F4 | `/community` 페이지 "불러오는 중..." 고착 | FN/UX | High | Fail |
| P0-F5 | `/create` 위저드 "로그인 확인 중..." 고착 | FN/UX | Critical | Fail |
| P0-F6 | `/api/bots/public` 500 — 공개 봇 카탈로그 불능 | AP/DB | Critical | Fail |

**총 6개 Fail 카드 자동 편입.**

---

## 게이트 판정

- RuntimeFailFlag: **true**
- 시스템 등급 자동 캡: **3 / 10**
- 리포트 품질 자동 캡: **60 / 100**
- S0 진입: 가능(Fail 6건 편입 전제)
- 진단 완료 선언: **불가** (6건 Fail 전부 Pass/Acceptable로 재진단 통과 필요)

---

## v3.0 진단과의 차이

v3.0 (2026-04-21 오전)은 이 9건 유령 컬럼과 4개 페이지 고착을 **0건** 발견했다. 정적 파서가 `SELECT` 문자열과 `CREATE TABLE` 컬럼 목록을 교차 대조하는 절차 자체가 없었기 때문. v4.0 P0-4가 이를 기본 절차로 편입한 결과 **30초 만에 9건 검출**.
